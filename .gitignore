# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# A zipped build, for handing a preview to someone. `dist` above does not
# cover it — that entry matches a directory name, not an archive of one.
dist.zip
*.local

# env files (can opt-in for committing if needed)
.env*

# turbo
.turbo

# storybook
storybook-static

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# typescript
*.tsbuildinfo

# claude code (local permission grants; launch.json is shared)
.claude/settings.local.json
